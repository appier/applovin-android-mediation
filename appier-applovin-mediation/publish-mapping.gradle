//TODO:  for proguard mapping file latter
//def uploadMappingFileByCurl(File mapping) {
//    exec {
//        //curl -v -H "X-Rollbar-Access-Token:7e6ba9aa10ff421eb80222deebb717a5" -F "version=7.23.0-LOCAL" -F "mapping=@mapping.txt" https://api.rollbar.com/api/1/proguard
//        workingDir projectDir
//        commandLine "curl", "-v", "-H", "X-Rollbar-Access-Token:$rollbarServerToken", "-F", "version=$version", "-F", "mapping=@${mapping.path}", "https://api.rollbar.com/api/1/proguard"
//    }
//}

static def getPublication() {
    String publication = System.getProperty("publication", "debug").toLowerCase()
    if (publication.contains("debug")) {
        return "debug"
    }
    return "release"
}

ext {
//    uploadMappingFile = this.&uploadMappingFileByCurl
    getPublication = this.&getPublication
}